apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-models-prod
  namespace: trading
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
