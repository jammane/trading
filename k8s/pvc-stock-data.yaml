apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-stock-data
  namespace: trading
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
