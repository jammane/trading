apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-models-prod
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /root/trading/models/prod
