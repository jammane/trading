apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-models-training
spec:
  storageClassName: manual
  capacity:
    storage: 15Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /root/trading/models/training
