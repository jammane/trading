apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-app-state
  namespace: trading
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
