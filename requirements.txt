# Install with CPU-only PyTorch wheel — see install_python.sh
torch==2.11.0
yfinance==1.2.1
alpaca-py==0.43.2
keyring==25.7.0
numpy==2.4.4
pytest>=8.0
matplotlib>=3.8
# Pinned exactly: ruff's rule set grows between releases, so an unpinned version
# silently changes what `ruff check .` reports and makes the lint gate meaningless.
ruff==0.15.22
